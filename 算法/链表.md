# 链表

## 翻转链表

``` java
public Node reverse(Node head){
    Node now = head;
    Node prev = null;
    while(null != now){
        Node next = now.next;
        now.next = prev;
        prev = now;
        now = next;
    }
    return prev;
}
```